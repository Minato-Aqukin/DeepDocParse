{
  "name": "@ddp/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.mjs",
  "description": "DeepDocParse Electron host for the shared Vue workbench",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.mjs",
    "smoke": "node scripts/smoke.mjs",
    "smoke:windows": "node scripts/smoke-windows.mjs"
  },
  "devDependencies": {
    "electron": "44.3.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
