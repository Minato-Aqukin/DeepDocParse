# 日志脱敏守卫的已审阅例外。
#
# 格式：生产源码相对路径:行号  # 为什么这一行安全
#
# 规矩：
#   - 只有"这一行确实打印了某个名字看着敏感、但值本身不敏感（或不能改）"
#     才配进这里，并且理由要能独立看懂；
#   - 例外必须仍然命中扫描器 —— 代码挪走/改掉之后这条例外会变成红，
#     逼人删掉它，而不是留成一条永久豁免；
#   - 新加例外要在同一个改动里说明它是怎么审的。
#
