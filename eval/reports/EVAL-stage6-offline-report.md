# Deep Agent 评测报表（mode=offline）

数据 revision：`stage6-offline-v1`。offline 是固定代理输出的结构评测，**不是模型真机质量**。

| 指标 | 结果 |
|---|---|
| 漏检率（该检索却没检索） | 16.7% (1/6) |
| 冗余检索率（可继承却又检索） | 16.7% (1/6) |
| 门控前引用精确率 | 50.0% (4/8) |
| 门控后引用精确率 | 80.0% (4/5) |
| 句级引用覆盖率 | 75.0% (3/4) |
| 句级引用正确率 | 66.7% (2/3) |
| 拒答正确率（改造前） | 75.0% (3/4) |
| 拒答正确率（Deep Agent） | 100.0% (4/4) |
| unsupported 不变式违反率 | 0.0% (0/4) |

门控精确率变化：50.0% → 80.0%（+30.0%）。
拒答正确率变化：75.0% → 100.0%（+25.0%，不得下降）。

GPU 批次二待补：用真实判定模型、embedding、rerank、chat/VQA 输出替换固定输出，
并按同一列定义报告 live 数字。
