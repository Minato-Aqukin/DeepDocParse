"""语料 worker：把长任务从 API 进程里搬出来。

合仓前索引、编译、抽取都跑在 FastAPI 的 `BackgroundTasks` 里 —— 也就是
**API 进程的内存**。三个直接后果：

1. 滚动发布把进程换掉，在途任务**静默消失**（企业边界 7）
2. API 副本的内存与 CPU 被长任务占着，扩容 API 等于扩容 worker
3. 想给编译单独限并发做不到 —— 它与请求处理共用同一个事件循环

现在它们住在这里，任务真相在 `corpus.tasks`（PG）。
"""
